demo.m: The main script. Run demo.m to see a quick example. It generates a simulated dataset, performs cross-validation, and fits the proposed model.

NonmonotoneAPG.m: The core solver using the Nonmonotone Accelerated Proximal Gradient algorithm.

proximal_operator_SCAD.m: The proximal operator for the SCAD penalty used in the optimization.
